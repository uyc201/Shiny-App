### Chapter 3 Exercise ###

#library
library(shiny)



####################################### Notes #######################################
#In your "app.R" script file, type "shinyapp" then press shift+tab to get the basic chunk of code ready for your shiny app
#When you get an error in the code/app running, first start by looking at the traceback()
#You can insert browser(). This is the standard R way of lauching the interactive debugger, and will work however you’re running Shiny.
#interactive debugger: the debugger pauses execution and gives you an interactive R console where you can run any code to figure out what’s gone wrong
#You can call the interactive debugger by clicking to the left of the line number (red dot). You can remove the breakpoint by clicking on the red circle

#Before you ask the community for some help, make sure you create a reprex, reproducible example